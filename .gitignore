# Secrets — never commit
.env
.env.*
!.env.example

# Python / Rasa local
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.rasa/
.pytest_cache/
.mypy_cache/

# Trained models (rebuild with `rasa train`)
models/*.tar.gz

# OS / IDE
.DS_Store
.idea/
*.swp

# Docker / local overrides
docker-compose.override.yml
