# Secrets — never commit
.env
.env.*
!.env.example

# Python / Rasa local
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.rasa/
.pytest_cache/
.mypy_cache/

# Trained models (rebuild with `rasa train`)
models/*.tar.gz

# OS / IDE
.DS_Store
.idea/
*.swp

# Local job state (rasa-versary sent receipts, etc.)
.data/

# Local caches / packaging leftovers
dist/
.cache/
.config/
.keras/
